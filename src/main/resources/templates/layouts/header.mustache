<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NBBANG</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        #header {
            text-align: center;
            padding: 20px;
        }

        #logo {
            width: 300px;
            height: auto;
            display: block;
            margin: 0 auto;
        }

        #underline {
            width: 100%;
            margin: 20px 0;
            border-bottom: 4px solid #4286f5;
        }

        form {
            width: 300px;
            padding: 20px;
            border: 1px solid #4286f5;
            border-radius: 10px;
            background-color: #ffffff;
            margin: 0 auto;
        }

        .mb-3 {
            margin-bottom: 15px;
        }

        .form-label {
            display: block;
            margin-bottom: 5px;
        }

        .form-control {
            width: 100%;
            padding: 8px;
            border: 1px solid #4286f5;
            border-radius: 10px;
            box-sizing: border-box;
        }

        .form-control:focus {
            outline: none;
            border-color: #4286f5;
            box-shadow: 0 0 5px rgba(66, 134, 245, 0.5);
        }

        .form-textarea {
            width: 100%;
            padding: 8px;
            border: 1px solid #4286f5;
            border-radius: 10px;
            resize: none;
            box-sizing: border-box;
        }

        #room-show {
            display: block;
            margin-top: 5px;
            border: 1px solid #4286f5;
            border-radius: 10px;
        }

        #create-room-button {
            width: 100%;
            padding: 10px;
            background-color: #4286f5;
            color: #ffffff;
            border: none;
            border-radius: 10px;
            cursor: pointer;
        }

        #create-room-button:hover {
            background-color: #3062a7;
        }
    </style>
</head>
<body>
<div id="header">
    <img id="logo" src="/img/logo.png" alt="logo">
    <div id="underline"></div>
</div>